export const Coffee = coffeeObject => {
    return `
        <article class="distributor">
            <h3>Distributor Name: ${cofeeObject}</h3>
            <section>Location: ${cofeeObject}, ${distributorObject.state}</section>
        </article>
    `
}